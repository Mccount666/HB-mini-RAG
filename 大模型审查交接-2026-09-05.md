# 大模型代码审查交接书

生成时间：2026-09-05
读者：即将接手本仓库独立审查的大模型（无前序对话上下文，本文自足）。
你的立场：上线前独立审查者。本仓库是面向**肝母细胞瘤患儿家长**的医疗问答系统，错误信息的代价极高，请从严审查。

---

## 一、系统速览

- **形态**：微信小程序（主）+ 网页测试版（web/，双端共用云函数）；另有同逻辑的自托管 Express 后端（backend/，当前未启用）。
- **调用链**：小程序 `pages/`+`services/` → `wx.cloud.callFunction` → 云函数 `cloudfunctions/qa`；网页 → HTTP 网关 → 同一云函数。
- **代码单一来源**：`cloudfunctions/qa/src/{rag,ocr,config.js}` 由 `tools/sync-cloudfunction.js` 从 `backend/src` 拷贝，`tools/check-sync.js` 校验零漂移。**改 backend 必须重新 sync + 部署**。
- **RAG 管线**（`backend/src/rag/`）：混合检索（余弦 512 维 + BM25，同义词扩展）→ 机械命中或 LLM 语义判定（judge.js 三分支）→ 严格提示词（DeepSeek，OpenAI 兼容可插拔）→ **生成后护栏（guard.js：引用校验 + 数字溯源）**。
- **知识库**：`backend/data/knowledge_base/*.json`（问答对模式）→ `npm run ingest` → `data/index.json`（当前 77 条 = 63 条已终审科普 + 14 条化验参考标准，`onlyReviewed:true`）。
- **HTTP 防护**（`cloudfunctions/qa/index.js` handleHttp）：共享密钥 `x-hb-secret`（timingSafeEqual）→ 每 IP 限速（实例内存 Map，20/分）→ CORS 白名单回显 → 错误统一通用文案。环境变量共 7 个（LLM_API_KEY / OCR_PROVIDER / TENCENT_SECRET_ID / TENCENT_SECRET_KEY / HTTP_SHARED_SECRET / HTTP_RATE_LIMIT / HTTP_CORS_ORIGINS）。

## 二、近期整改记录（避免重复报告）

整改清单原文：`整改清单-2026-09-04.md`（P0~P3 逐项）。已落地并部署的提交（git log 可查）：

| 提交 | 内容 |
|---|---|
| 95980de | 上线门控收紧至 77 条已审核 + HTTP 端点防护 + 终审双条件（reviewed===true 且 reviewedBy 非空） |
| d22d967 | P1/P2/P3 全落地：embedder 超时重试+维度三重校验、chat 输入清洗（2000 字截断+history 白名单）、feedback GET 管理令牌、CORS 白名单、阈值统一 0.50、云函数错误脱敏、历史详情页 |
| 2637f1c | 护栏数字溯源 3 处边界修复（^ 锚点偏移 / 小数点正则通配 / 相邻数字吞边界）+ **中文单位等价类**（毫克↔mg、毫升↔mL↔ml 等，堵住"100 毫升撞 100mg"的单位错换漏拦）+ 详情页 pre-wrap |
| 63ca6e9 | 依赖：overrides 强制 qs≥6.16.0（2 条 DoS）；uuid 通告评估不可达接受（SDK 零调用） |
| 9d9f39d | 网页配套：config.js 密钥、web/data/index.json 换 77 条门控版 |

线上验证已做（勿重复劳动）：云函数直调冒烟 1.5s 带引用；网关防护 8/8（无/错密钥 401、带密钥 200、CORS 精确回显、非白名单无 CORS 头、限速第 21 请求 429）；静态托管与 gh-pages 均已更新验证。`node tools/qa-gw-test.js` 可复跑网关验证。

## 三、审查重点（按优先级，含我们怀疑存在但未修的问题）

### A. 护栏可绕过路径 — `backend/src/rag/guard.js`（医疗安全最后防线）
先读 `checkGroundedNumbers` / `extractCriticalNumbersDetailed` / `consumeUnit` / `unitClass`。已有回归测试在 `backend/test/rag.test.js`。
**我们明确怀疑、请优先验证的缺口**：
1. **全角数字与中文数字绕过**：数字提取正则是 `/\d+(?:\.\d+)?/`，"５００mg"（全角）或"五百毫克"不会被提取 → 不做溯源检查直接放行。模型被要求写阿拉伯数字，但注入或幻觉下呢？是否应在提取前做全角→半角与中文数字规范化？
2. 单位等价类缺口：mmol/L、U/L、×10⁹ 上标变体、全角％；单位前有其他标点的形态。
3. `validateCitations` 剔除越界引用后 citationCount=0 走 no_citations 重试一次——重试提示词约束是否足够，重试结果是否仍过护栏？
4. 拒答/学习话术（LEARNING_FALLBACK，answer.js）中的固定文案是否可能被注入内容拼接污染。

### B. 提示词注入面 — `backend/src/rag/answer.js` + `prompt.js` + `routes/chat.js`
- 用户 message（≤2000 字）会进入生成提示词；history 走结构白名单（仅 user/assistant、各 ≤2000 字、≤6 轮）。
- 请构造"忽略之前指令/扮演医生"类 payload 评估实际防线：角色分隔符、系统提示词强度、护栏兜底是否闭环。
- `buildRetrievalQuery` 追问拼接、`expandText` 同义词扩展是否可被操纵检索。

### C. 云函数 HTTP 入口 — `cloudfunctions/qa/index.js`
- `secretOk` 长度前置比较泄露密钥长度（timingSafeEqual 前置），可否接受？
- `rateLimited` 为单实例内存计数——云开发多实例/冷启动下可被绕过（已知取舍，请评估实际风险等级）。
- OCR 分支 `cloud.downloadFile({fileID})` 对 fileID 无约束：登录用户可让函数读自己环境内任意云存储路径（含他人上传件），是否有越权读取面？
- `handleFeedback` 任何人可写 feedback 集合（设计如此），直调路径无限速——写入洪水风险。
- `parseHttpBody` JSON 解析深度/大小限制（网关侧有上限？）。

### D. 知识库门控 — `backend/src/kb/index.js`
- `isReviewed` 双条件 + `category==='lab_reference'` 无条件白名单（14 条参考区间，OCR 硬依赖，source 注明"待导师核对"）。评估：往 lab_reference.json 塞一条带 reviewed:false 的科普条目即可绕过门控流向家长——这个例外是否应收紧为"lab_reference 且带客观区间结构"？

### E. 低优先
- `backend/src/routes/auth.js` 是明示占位（自托管未启用）；`routes/feedback.js` GET 有 ADMIN_TOKEN 门（Mimosa 静态分析误报为无防护，可复核）。
- `web/rag-client.js`（浏览器移植）与 backend 检索参数一致性（threshold 0.50）。

## 四、已接受的取舍（可以挑战，但请给出场景与代价）

1. 无单位纯数字只查词边界（数值>20 才查）——防过度拒答。
2. 分期罗马数字（Ⅰ/Ⅱ/Ⅲ期）不在数字溯源范围。
3. uuid <11.1.1 依赖通告接受（tencentcloud SDK 声明但零调用）。
4. web 端密钥公开可见（软防护，真拦截靠限速；设计注释见 web/config.js）。
5. lab_reference 免终审白名单（见 D，若你认为应收紧请给方案）。
6. 限速为尽力而为的单实例内存实现。

## 五、自验命令（不需要任何密钥即可跑）

```bash
cd backend
npm test            # 29/29 通过（node --test，mock LLM 不触网）
npm run check:sync  # backend 与云函数零漂移
npm run eval        # 检索阈值校准 26/26（local embedding，免费）
node tools/qa-gw-test.js   # 网关 8 项防护复验（会打线上，限速用例自动等待重试）
node tools/qa-live-test.js # 14 个真实问题打线上（IN/OUT/NEAR/HALLU 场景；消耗 DeepSeek 少量费用）
```

静态扫描现状：Mimosa deep scan（scan-2026-09-05T13-57-19.441Z-08a17fc3489a）2 条 candidate 均已判读（auth 占位已知 / feedback 误报）；npm audit 仅剩已接受的 uuid。

## 六、产出要求

每条 finding 一行结论 + 详情：`[P0|P1|P2] 标题 | 文件:行号 | 可复现证据或攻击 payload | 建议修法 | 误报风险自评`。
无法证实的猜测标"待验证"并说明验证路径。最后给一段总体结论：可否上线、上线前置条件清单。
