# 肝母细胞瘤科普问答小程序 · 架构评审整改指令书

**评审日期**：2026-09-11
**评审对象**：`hepatoblastoma-qa-miniprogram`（appid `wx92ba1d52a0eb2e43`）
**评审方式**：全量源码逐文件审阅 + 数据面实测（知识库 167 条、线上索引一致性、目录体积统计）
**评审人**：微信小程序开发工程师（架构评审）
**给谁看**：负责本项目的开发 Agent

---

## 〇、阅读须知

本文件是**整改指令书**，不是表扬信。每条问题都给出了**精确文件路径 + 行号 + 当前代码 + 建议改法**，可直接执行。

请按 **P0 → P1 → P2** 顺序处理。**P0 未清空之前不要提交审核。**

同时请注意：本文件**不含任何代码改动**，评审与修改分离。改之前先读完整份，避免局部改动引入新的漂移（本项目已有一次文档/代码漂移的历史，见 P1-2）。

---

## 一、总体评价（先说结论）

**架构定级：上游水平，不需要推倒重来。**

分层干净、职责单一、反幻觉工程真实落地，在我审过的同体量医疗科普小程序里属于少数派。以下四点是**真的做到了**，不是 PPT 架构：

### 1.1 四层架构无循环依赖

```
① 小程序前端 (pages / components / services / utils)
        ↓ wx.cloud.callFunction({name:'qa'})
② 云函数 qa（唯一后端形态，双入口）
        ↓ sync-cloudfunction.js 单向拷贝
③ 共享 RAG 核心 backend/src/{rag, ocr, config.js}
        ↓ 可插拔
④ 外部依赖（DeepSeek LLM / 本地哈希 embedding / 腾讯云 OCR 或 mock）
```

核心价值：**同一套 RAG 逻辑同时服务小程序端（云函数）与自托管 web 端（`web/rag-client.js`）**，一份逻辑两个出口。这是正确的工程取舍。

### 1.2 反幻觉六道防线是真刀真枪写的

重点核查 `backend/src/rag/guard.js`，确认以下机制**均已落地**：

- `UNIT_EQUIV` 单位等价类（mg↔毫克、ml↔mL↔毫升）
- `consumeUnit()` 多字符单位优先匹配 + 斜杠复合单位处理
- `normalizeNumbers()` 全角数字 → 半角、中文数字+单位 → 阿拉伯数字
- `checkGroundedNumbers()` 带词边界环视（避免 `7` 误匹配 `17`）
- `guardAnswer()` 分级处理：`ungrounded_numbers` **不重试直接拒答**，`no_citations` 允许重试一次

这个分级是正确的。数字溯源失败属于硬错误，重试无意义；引用缺失属于可修复，允许一次强化重试。

### 1.3 数据面健康，终审门控无漏洞

实测结果：

| 检查项 | 结果 |
|---|---|
| 线上索引 `cloudfunctions/qa/data/index.json` | `onlyReviewed: true`、`dim: 512`、`count: 167` |
| 本地知识库 5 个 JSON 合计 | **167 条，167 条 reviewed** |
| 一致性 | 线上线上索引与本地源**逐条对得上** |
| `kb/index.js` `isReviewed()` | **双条件** `reviewed === true && reviewedBy.trim()` |

最后一条是加分项。很多项目只判断一个布尔值，本项目要求 `reviewedBy` 必须非空——意味着**「谁审的」必须留痕**。这是正确的医疗内容治理姿势。

### 1.4 包体控制优秀

| 目录 | 体积 |
|---|---|
| pages | 27 KB |
| components | 11 KB |
| services | 4 KB |
| utils | 2 KB |
| assets | 1 KB |
| **前端合计** | **45 KB** |

主包 2MB 限制下连零头都不到，**完全不需要分包**。`project.config.json` 的 `packOptions.ignore` 已排除 backend/web/docs/tools/知识库备份，`lazyCodeLoading: requiredComponents` 已开启。这块没有可挑的。

---

## 二、问题清单

### P0 — 上线前必须堵洞（2 项）

---

#### P0-1 知识库存在机器翻译残留，会直接进入 LLM 上下文

**文件**：`backend/data/knowledge_base/lab_reference.json`
**条目**：`LAB-AST`
**当前内容**：

```json
{
  "id": "LAB-AST",
  "question": "天冬氨酸氨基转移酶(AST)的参考范围与意义",
  "answer": "天冬氨酸氨基转移酶（AST，旧称 GOT）存在于肝、心肌、肌肉等，升高除肝脏外也可见于肌肉损伤。成人常见参考区间约 13–35 U/L 或 15–40 U/L。AST/ALT  together 辅助判断肝损伤。以报告单标注区间为准。",
  "reviewed": true,
  "reviewedBy": "参考标准入库"
}
```

**问题**：`AST/ALT  together` 中的 `together` 是英文机器翻译残留（应为「联合」「共同」）。

**为什么这是 P0 而不是排版问题**：

1. 该条文本会被 `retriever` 命中
2. 命中后作为 `<知识库>` 内容拼进 prompt 送进 DeepSeek
3. 模型有概率原样复述这个词，输出给患儿家长

对一个医学科普产品，「AST/ALT together」这种中英夹杂会**直接摧毁专业信任**。这不是审美问题，是内容事故。

**全库扫描结果**：仅此一处真污染。另有两处 `Together by St. Jude`（`web_collected.json` 的 `HB-254`、`HB-255`）是机构专名，属正常，**不要改**。

**建议改法**：

```diff
- AST/ALT  together 辅助判断肝损伤。
+ AST/ALT 比值可辅助判断肝损伤类型。
```

同时**顺手改一下 `reviewedBy`**，从 `参考标准入库` 改为具体责任人——现在这个值等于没有留痕，而 `isReviewed()` 的双条件设计本意就是要留痕。

**注意**：改完必须**重新跑 `npm run ingest`** 重建索引，并**重新执行 `npm run sync` + 上传云函数**。只改 JSON 不重建索引，线上检索到的还是旧文本。

---

#### P0-2 `feedback` 写入无任何限制，是可被灌爆的计费洪水面

**文件**：`cloudfunctions/qa/index.js`
**相关行号**：`handleFeedback` 定义在 **128-147 行**

**当前实现**：

```javascript
128: async function handleFeedback(data = {}) {
129:   const rec = {
130:     q: String(data.q || '').slice(0, 300),
131:     rating: data.rating === 'good' ? 'good' : 'bad',
132:     comment: String(data.comment || '').slice(0, 500),
133:     mode: data.mode === 'ai' ? 'ai' : 'demo',
134:     topicKey: String(data.topicKey || '').slice(0, 40),
135:     topicLabel: String(data.topicLabel || '').slice(0, 40),
136:     ts: new Date().toISOString(),
137:   };
138:   try {
139:     const db = getCloud().database();
140:     await db.collection('feedback').add({ data: rec });
141:   } catch (err) { ... }
142:   ...
147: }
```

**问题**：`handleFeedback` 内部**没有任何鉴权、没有限速、没有调用方归属校验**。

**更严重的是调用路径分析**（这是我审查中发现的、比表面看起来更麻烦的一点）：

- **HTTP 入口**（`handleHttp`，第 275-277 行）：走 `secretOk()` + `rateLimited(ip)`，**有保护**
- **小程序直调入口**（`exports.main`，第 293-297 行）：

```javascript
293:   const id = callIdentity(event, context);
294:   if (rateLimited('call:' + id)) {
295:     return { answer: '', sources: [], error: '请求太频繁，请稍后再试' };
296:   }
297:   return handleCall(event, context);
```

第 293-297 行只做了**调用身份限速**，然后直接进 `handleCall`。而 `handleCall` 内部（第 56-58 行）会识别 `event.type === 'feedback'` 并转给 `handleFeedback`。

**结论**：小程序直调路径能写 feedback，且**只受「每分钟 20 次」这一层限制**。攻击者用脚本调用（伪造/复用合法小程序会话、或直接打 HTTP 入口绕开）即可高速灌注。

**为什么这是钱的问题**：`feedback` 集合按云开发计费，写入量由攻击者控制，**账单由项目方承担**。同时 `feedback` 里存的是用户原始问题（`q`），被灌水后真实反馈完全无法分辨，这个数据源直接废掉。

**建议改法（按成本从低到高）**：

**最低成本（建议先做）**：在 `handleFeedback` 入口加一层按调用方身份的低频限速 + 强制长度硬截断。

```javascript
async function handleFeedback(data = {}, identity = '') {
  // 反馈类写入单独限速：每身份 5 次/分钟（复用 rateLimited，走独立桶前缀）
  if (rateLimited('fb:' + (identity || 'anon'), 5)) {
    return { ok: false, error: '提交过于频繁' };
  }
  // 其余不变...
}
```

并在两处调用点传入 identity：
- 第 58 行 `handleCall` 内：传入 `callIdentity(event, context)`
- 第 276 行 HTTP 入口：传入 `getSourceIp(event)`

同时给 `rateLimited` 加一个可选 limit 参数（当前是硬编码读 env，见 202-216 行）。

**中等成本**：把限速计数从「实例内存 Map」挪到云数据库（原子自增 + TTL 集合），解决多实例绕过问题（详见 P1-1）。

**额外建议**：`feedback` 集合设置**数据保留期**（如 90 天自动清理），避免无限增长。云开发支持 TTL 索引。

---

### P1 — 应修，但不阻塞上线（3 项）

---

#### P1-1 限速基于单实例内存，多实例水平扩容下形同虚设

**文件**：`cloudfunctions/qa/index.js`
**相关行号**：`rateBuckets` 与 `rateLimited` 定义在 **200-216 行**

**当前实现**：

```javascript
200: // 每 IP 每分钟 POST 限速（实例内存计数；实例回收即清零，属尽力而为的防刷）
201: const rateBuckets = new Map(); // ip -> { count, ts }
202: function rateLimited(ip) {
203:   const limit = parseInt(process.env.HTTP_RATE_LIMIT || '20', 10);
```

代码注释**自己已经承认了**这是「尽力而为的防刷」。这个诚实是好的，但需要明确它的实际强度：

**机制缺陷**：
1. **实例内存**：CloudBase 云函数会水平扩容。攻击者并发打，请求分散到 N 个实例，每个实例独立计数。
2. **冷启动清零**：实例回收后 `rateBuckets` 归零，计数从 0 重新开始。
3. **实际限速** ≈ `N × 20 / 分钟`，N 由攻击者并发量决定，**不是 20**。

**次要问题**：按 IP 计数在移动网络下误伤严重（运营商 NAT 出口共享），中国移动网络尤其明显。**同一出口 IP 下可能是一整个小区/写字楼**，正常用户会被连带限速。

**建议改法**：

- **短期**：把主键从 IP 改为「IP + OPENID 组合」，或对小程序直调路径完全用 OPENID 计数（已有 `callIdentity`），HTTP 路径才用 IP。
- **长期**：限速计数落到云数据库（集合 + 原子自增 + 到期清理），或使用云开发的原子操作实现跨实例共享计数。
- **兜底**：在云函数层面配置**并发上限**，把最坏情况下的成本封顶。这一条**比限速本身更重要**——限速可以绕，并发上限绕不掉。

---

#### P1-2 文档口径三处漂移，其中一处是给用户看的

**漂移 1：检索阈值**

| 位置 | 行号 | 内容 |
|---|---|---|
| `backend/src/config.js` | 45-47 | `embeddingProvider === 'local' ? '0.50' : '0.45'` |
| `docs/architecture.md` | 63 | 「默认阈值 **0.35**」 |
| `docs/architecture.md` | 64 | 「若改用 API embedding，默认 0.45」（**这条是对的**） |

**`docs/architecture.md` 第 63 行的 0.35 是错的，真实值是 0.50。** 整改清单 P2-1 已提过这件事，但文档没有同步修改。

**危害**：阈值是控幻觉的核心旋钮。后续维护者按 0.35 去调参或复用，会得到完全错误的结论。而且文档还写着「跑 eval 校准」——如果连默认值都记错了，校准的基线就是错的。

**建议改法**：把 `docs/architecture.md` 第 63 行的 `0.35` 改为 `0.50`。

**漂移 2：知识库条数**

| 位置 | 行号 | 内容 |
|---|---|---|
| `pages/about/about.wxml` | 19 | 「已完成儿科肿瘤专业导师终审，正式收录 **167 条**」 |
| `README.md` | 156 | 「随时跑 `node tools/kb-status.js` 看审核进度（当前共 **44 条待审**）」 |
| `README.md` | 151 | 「全部条目均为 `reviewed:false` 的**演示数据**」 |
| `OVERVIEW.md` | — | 「替换为经审核的知识库」（未给出数字） |

**四个口径，互相矛盾。** 实测真相是：**167 条，全部 reviewed，无待审**。

**其中 `about.wxml` 第 19 行最要命**——这是**渲染给用户看的页面**。对患儿家长来说「正式收录 167 条」是有分量的承诺。问题在于：

1. **数字写死在 wxml 里**，知识库一更新它就过期，且不会有人记得改
2. 一旦线上实际条数与页面所述不符，属于**对用户的不实陈述**，在医疗类目下风险更高
3. README 第 151、156 行的表述是项目早期的状态，早已过期，会误导接手的人以为知识库还是演示态

**建议改法**：

- `pages/about/about.wxml` 第 19 行：**去掉硬编码数字**，改为不带数字的表述，例如「已完成专业终审，收录肝母细胞瘤科普、治疗、护理、随访与化验参考内容」。若确实要展示条数，应由云函数/配置文件下发，而非写死。
- `README.md` 第 151、156 行：更新为实际状态（167 条全部已终审），或删除过期的「演示数据」段落。
- 建立规则：**知识库条数只在一处定义**，其余引用该来源。

**漂移 3：云函数依赖声明与实际不一致**

`cloudfunctions/qa/package.json` 声明了 `dotenv ^16.4.5`，但 `cloudfunctions/qa/node_modules/` 中**实际缺失**（MISSING）。

影响：`cloudfunctions/qa/src/config.js` 用 `try { require('dotenv').config(); } catch (e) {}` 兜住了，不会崩。但这是「声明了没装」的状态，说明云函数依赖目录**没有被正确安装/校验过**。详见 P2-1。

---

#### P1-3 `minCoverage` 默认关闭（0），近邻问题完全依赖提示词与护栏兜底

**文件**：`backend/src/config.js`
**行号**：52

```javascript
52: minCoverage: parseFloat(process.env.RETRIEVAL_MIN_COVERAGE || '0'),
```

**代码注释已经解释了为什么关闭**（48-51 行）：

> 口语化提问（「出院后多久复查一次」）的日常用词同样查不到，覆盖率天然偏低，无法与「糖尿病饮食」这类近邻问题统计分隔，因此只作为 eval 观测指标；近邻问题由「严格提示词拒答 + 生成后护栏」两道防线兜底。

**这个判断本身是合理的**，我不建议无脑打开。但需要明确其**风险承担方**：

`minCoverage=0` 意味着「只命中一个边角词」也会被判定为有效召回，交给 LLM。此时：
- 提示词防线的强度取决于模型是否听话（DeepSeek 在 `temperature: 0` 下相对稳定，但非绝对）
- 护栏防线只拦「数字无出处」和「无引用」，**拦不住「有引用但答非所问」**

也就是说：**近邻问题的最后一道防线是模型的自觉性**。对医疗科普，这个兜底偏薄。

**建议改法（不要直接改成非零）**：

1. **先用数据说话**：跑 `npm run eval`（`tools/eval.js`），对同一批测试问题分别取 `minCoverage = 0` 和 `0.3`，比较**误拒率**与**近邻误答率**两条曲线
2. 只有在数据支持「0.3 能显著降低近邻误答、且误拒率可接受」时才启用
3. 若启用，建议**保留 `0` 作为默认、通过 `.env` 在特定场景开启**，而非改代码默认值

**这条的优先级低于前两条**，但它决定了产品上线后是「偏冷但安全」还是「偏热但有风险」，需要你拍板。

---

### P2 — 工程卫生，建议集中清理（4 项）

---

#### P2-1 云函数 `node_modules` 体积失控（65 MB），且未被忽略规则明确覆盖

**实测体积**：

```
cloudfunctions/qa/node_modules    65,151 KB  (约 65 MB，138 个顶层包)

体积 top:
  @typescript                      27,700 KB   ← 纯类型定义，运行时无用
  protobufjs                       14,534 KB   ← tencentcloud-sdk 传递依赖
  bson                              2,808 KB
  @types                            2,465 KB   ← 纯类型
  typescript                        2,439 KB   ← 编译器，运行时无用
  @cloudbase                        1,583 KB
  lodash                            1,381 KB
  ajv                                 916 KB
  tencentcloud-sdk-nodejs-ocr         836 KB
  tcb-admin-node                      824 KB
```

**问题分析**：

1. `@typescript` + `@types` + `typescript` 合计约 **32.6 MB**，占总体积一半——**这些是开发期类型定义和编译器，运行时完全不需要**。出现这个现象说明本地执行了 `npm install`（含 devDependencies）而非 `npm install --production`。
2. 不影响线上（微信侧有自己的打包规则），但严重影响**本地上传速度与部署体验**
3. **`.gitignore` 覆盖不明确**：当前规则是 `node_modules/`（无前导斜杠）。按 gitignore 语义，无斜杠的 `node_modules/` 会匹配任意层级，所以 `cloudfunctions/*/node_modules/` **实际是被忽略的**。但语义不清晰——**建议显式补上 `cloudfunctions/*/node_modules/`**，避免其他工具（打包脚本、CI）误判。

**另：** `cloudfunctions/qa/package.json` 里 `"main": "index.js"` 后面没有 `"scripts"` 段，无法通过 `npm run` 触发任何本地操作。建议补一个 `"pretest"` 或 `"prepare"` 钩子做依赖校验。

**建议改法**：

- 清理：删除 `cloudfunctions/qa/node_modules`，改用 `npm install --production`（或在部署流程中由云开发平台自动安装）
- `.gitignore` 显式添加 `cloudfunctions/*/node_modules/`
- 补上 `dotenv` 的实际安装（或从 `package.json` 中移除该声明，因为云函数环境本就无 `.env`）

---

#### P2-2 死代码 `utils/request.js`

**文件**：`utils/request.js`
**状态**：文件头自述「⚠️ 当前未接入」，`BASE_URL = 'https://your-api-domain.com'`，**全项目零引用**（已确认无任何 `require` 指向它）。

**危害**：会误导后来者以为项目存在自托管后端调用路径。整改清单 P3-2 已建议清理，未执行。

**建议改法（二选一）**：

- **方案 A（推荐）**：删除，并在 `docs/architecture.md` 里说明「未接入自托管后端，所有请求经云函数」
- **方案 B**：移入 `docs/examples/` 作为未来自托管参考，文件头补上「未启用」标注

**同样处理**：`utils/` 下若还有 `auth.js` 等同类占位文件，一并核查。

---

#### P2-3 单一来源同步机制缺乏强制校验，存在静默分叉风险

**机制现状**：

| 组件 | 作用 |
|---|---|
| `tools/sync-cloudfunction.js` | 把 `backend/src/{rag, ocr, config.js}` + `backend/data/index.json` 拷入 `cloudfunctions/qa/` |
| `tools/check-sync.js` | 校验两边是否一致，`exit 1` = 有漂移（72 行） |
| `backend/package.json` `predeploy` | `sync && check:sync` |

**问题**：

1. `check-sync.js` **只能手动跑**。`predeploy` 是**本地 npm 脚本，可以被绕过**（直接上传云函数即可）。
2. **没有任何 CI / git hook 强制校验**。只要有人改完 `backend/src/rag/*.js` 忘了 `npm run sync`，线上云函数就跑旧代码，**且没有任何报错**。
3. 结合本项目历史——已经出现过一次文档/代码漂移（P1-2）——这个风险是**已被验证会发生的**，不是理论风险。

**这是本项目当前最值得补的工程短板**：静默分叉意味着「你测的是 A，线上跑的是 B」。

**建议改法（按成本从低到高）**：

- **低成本**：在 `backend/package.json` 加 `pretest` 钩子：`"pretest": "node ../tools/check-sync.js"`。这样跑测试前自动校验——**任何人改完代码跑测试就会立刻发现漂移**。这是性价比最高的一步。
- **中成本**：加 git pre-commit hook（`.husky` 或 `.git/hooks/pre-commit`），提交前跑 `check:sync`。
- **彻底方案**：把 `cloudfunctions/qa/src` 从版本控制中排除（作为构建产物），部署流程强制先 sync。这样**物理上不可能分叉**。

**另建议**：`check-sync.js` 现在只比对内容是否一致。可以考虑增强为「比对 + 报告差异详情」，便于快速定位。

---

#### P2-4 OCR 解读路径不做数字溯源（当前是有意设计，但需补一道约束）

**文件**：`backend/src/ocr/interpret.js`（共 49 行）

**当前实现**：

```javascript
 9: const { validateCitations } = require('../rag/guard');
35: const { answer: interpretation } = validateCitations(rawInterpretation, hits.length);
```

**确认**：OCR 解读路径**只校验引用（`validateCitations`），不做数字溯源（`checkGroundedNumbers`）**。

**设计理由（合理）**：化验单解读的数值来自家长上传的报告本身，溯源到知识库没有意义——知识库里不可能有「这个孩子的具体数值」。

**但这个设计留下一个真实风险**：LLM 在解读时可能**顺口补充一个知识库没有的数字**。例如家长报告上写「ALT 85」，模型输出「ALT 85 U/L，明显高于正常上限 40 U/L」——这个 `40` 如果知识库那条参考区间写的是 `7-40` 倒还罢了，但如果写的是 `9-50`，模型凭空给的 `40` 就**没有任何出处**。此时护栏是关着的。

**建议改法（三选一）**：

- **方案 A（最轻）**：在 `OCR_SYSTEM` 提示词里加硬约束——「不得输出家长报告与知识库参考区间之外的任何数字；如需说明正常范围，必须逐字引用知识库原文」。
- **方案 B（推荐）**：对 `interpret` 的输出做一次**双向校验**——输出的每个数字必须能在（家长报告原文 ∪ 命中的知识库条目）中找到。命中不了则剔除该句或整条拒答。可复用 `guard.js` 的 `extractCriticalNumbersDetailed()`，把「来源集合」从知识库扩展为两者并集。
- **方案 C**：接受现状，但在前端明确标注「本解读基于您上传的报告与通用参考区间，具体以医生判断为准」。（当前 about 页已有类似免责声明，但 OCR 结果卡片上是否有，需确认。）

**取舍建议**：方案 B 实现成本不高（复用现有函数），收益明确，建议做。

---

## 三、问题汇总表

| 编号 | 级别 | 位置 | 问题 | 修复成本 |
|---|---|---|---|---|
| P0-1 | **P0** | `lab_reference.json` LAB-AST | 机器翻译残留 `together` 进入 LLM 上下文 | 极低 |
| P0-2 | **P0** | `cloudfunctions/qa/index.js` 128-147 | feedback 写入无鉴权/无独立限速，可被灌爆计费 | 低 |
| P1-1 | P1 | `cloudfunctions/qa/index.js` 200-216 | 限速为单实例内存，多实例可绕；按 IP 计数误伤 | 中 |
| P1-2 | P1 | `docs/architecture.md` 63、`about.wxml` 19、`README.md` 151/156 | 文档口径三处漂移，其中一处面向用户 | 极低 |
| P1-3 | P1 | `backend/src/config.js` 52 | `minCoverage=0`，近邻问题最后防线是模型自觉 | 需 eval 定标 |
| P2-1 | P2 | `cloudfunctions/qa/node_modules` | 65MB 含 32MB 类型定义；dotenv 声明未装 | 低 |
| P2-2 | P2 | `utils/request.js` | 死代码，零引用 | 极低 |
| P2-3 | P2 | `backend/package.json` | 无强制同步校验，存在静默分叉风险 | 低 |
| P2-4 | P2 | `backend/src/ocr/interpret.js` 35 | OCR 解读无数字溯源 | 中 |

---

## 四、执行顺序建议

**第一批（处理完即可提交审核）**
1. P0-1：改 `LAB-AST` 文本 → **重新 `npm run ingest`** → **`npm run sync`** → 上传云函数
2. P0-2：给 `handleFeedback` 加独立限速与长度硬截断
3. P1-2：修三处文档口径（成本极低，顺手做掉）

**第二批（上线后第一周）**
4. P2-3：加 `pretest` 钩子跑 `check:sync`（性价比最高的工程补强）
5. P1-1：限速改造（短期先做 IP+OPENID 组合键）+ 配置并发上限

**第三批（迭代期）**
6. P2-1、P2-2：依赖清理与死代码清理
7. P1-3：跑 eval 定标 `minCoverage`
8. P2-4：OCR 数字溯源补强

---

## 五、变更时容易踩的坑（给执行者的提醒）

本项目已有一次文档/代码漂移的历史，以下四点**每次改动都要过一遍**：

1. **改知识库必重建索引**：改 `backend/data/knowledge_base/*.json` 后不跑 `npm run ingest`，线上检索到的还是旧文本
2. **改核心逻辑必同步云函数**：改 `backend/src/**` 后不跑 `npm run sync`，云函数跑的是旧代码，且**无任何报错**
3. **改条数必同步四处**：知识库条数在 `about.wxml`、`README.md`、`OVERVIEW.md`、实际数据中都有出现，**建议收敛为单一来源**
4. **改配置默认值必同步文档**：`config.js` 的默认值与 `docs/architecture.md` 的记录已出现一次漂移（0.50 vs 0.35），本次修改后请一并核对

---

## 六、评审结论

**架构本身可以上线，代码质量在及格线以上，反幻觉工程做得比多数商用 RAG 扎实。**

问题集中在两类：

- **内容风险**（P0-1）：一处残留污染，改一个词，但不改就是硬伤
- **安全边界**（P0-2 / P1-1）：feedback 无防护、限速可绕，属于「知道有洞但可能决定先过」的取舍——**这一条需要你明确拍板，而不是默认忽略**

工程化层面（P2-3 静默分叉）是**长期最值得投入的一条**：本项目已经在文档层面漂移过一次，代码层面的同步机制不应该继续依赖人工自觉。

**建议：先做第一批（P0 + 文档口径），再提交审核。**

---

*本文档仅为评审结论与整改建议，未对任何代码或文档执行修改。*
