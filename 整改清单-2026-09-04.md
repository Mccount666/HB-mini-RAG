肝母细胞瘤问答小程序 · 代码整改清单
生成时间：2026-09-04
说明：以下为纯分析后整理的整改项，未改动任何业务代码。按优先级 P0（上线前必改）→ P3（清理）排列。

================================================================
P0 — 上线前必改（医疗安全 / 合规风险）
================================================================

【P0-1】重建仅含已审核内容的检索索引
- 问题：已部署的 cloudfunctions/qa/data/index.json 元数据 onlyReviewed=false，167 条中 104 条 reviewed!==true（约 62% 为未审核演示/草稿），当前正流向患儿家长。
- 关键坑：KB_ONLY_REVIEWED 只在 ingest 构建期消费，云函数运行时不加载 kb 模块。在云函数控制台设该环境变量无效。
- 整改动作：
  1) 在 backend 目录执行：KB_ONLY_REVIEWED=true npm run ingest
  2) node tools/sync-cloudfunction.js
  3) 微信开发者工具右键 cloudfunctions/qa → 上传并部署
  4) 上线前确认 index.json 的 onlyReviewed===true 且未审核条目为 0

【P0-2】公开 HTTP 端点加鉴权与限速
- 问题：cloudfunctions/qa/index.js 的 handleHttp / CORS_HEADERS 无 token 校验、无限速、Access-Control-Allow-Origin 通配。网页版 AI 模式依赖此 /api/chat，任意人可免密刷量，烧 LLM 费用且背合规锅。
- 整改动作：
  1) 加共享密钥或 Referer 校验（网页版请求带固定 header/参数）
  2) 加速率限制（按 IP/调用方限频）
  3) CORS 不要 * 通配，仅放开网页版部署域名

================================================================
P1 — 高优先（功能缺陷 / 安全）
================================================================

【P1-1】OCR 腾讯云 provider 依赖缺失（启用即崩溃）
- 位置：backend/src/ocr/ocr.js（require('tencentcloud-sdk-nodejs-ocr')）；backend/package.json
- 问题：package.json 未声明该依赖，OCR_PROVIDER=tencent 时运行时抛 Cannot find module，整条化验单管线崩。
- 整改动作：
  1) 在 backend/package.json 的 dependencies 加入 tencentcloud-sdk-nodejs-ocr（版本与 qa/package.json 对齐，约 4.0.0）
  2) 或在 ocr.js 对该 require 做 try/catch 优雅降级回 mock 并报明确错误

【P1-2】审核门控弱于设计契约
- 位置：backend/src/kb/index.js（onlyReviewed 过滤逻辑）
- 问题：仅判 !!item.reviewed，未强制 reviewedBy / reviewedAt。README 与架构文档明确"reviewed=true 且 reviewedBy 非空才算导师终审"。
- 整改动作：过滤条件改为 reviewed===true && !!reviewedBy（必要时 also reviewedAt），把终审契约钉死。

【P1-3】自托管后端安全加固（若计划启用）
- 位置：backend/src/index.js、backend/src/routes/auth.js、backend/src/routes/feedback.js、backend/src/rag/prompt.js & judge.js
- 问题：
  a) auth.js 是 stub，crypto.randomBytes 随机 token 从不校验，无鉴权中间件
  b) index.js 的 cors() 通配
  c) GET /api/feedback 无鉴权返回家长提问 q 与评论 comment（PII 泄露）
  d) 用户 message 与客户端可控 history 原样拼进提示词，无清洗/长度上限（提示词注入面）
- 整改动作（若启用自托管才需做）：实现微信登录并校验 token；cors 限制为小程序/网页域名；feedback GET 加鉴权；对 message/history 做长度截断与结构化隔离（分隔符/角色约束）。

================================================================
P2 — 中优先（质量 / 健壮性）
================================================================

【P2-1】检索阈值配置漂移
- 位置：backend/src/config.js:43（默认 0.50） vs backend/.env.example:34（写 0.35）
- 问题：二者矛盾，照抄 .env.example 会得到更低阈值 → 更易误召回、幻觉上升。
- 整改动作：统一为 0.50（与实测校准结论一致），并同步改 .env.example 与 config.js 注释。改完用 node tools/eval.js 复跑校准。

【P2-2】embedder 的 api 路径兼容性与 llm 不一致
- 位置：backend/src/rag/embedder.js（apiEmbed 用全局 fetch） vs backend/src/rag/llm.js（用 node:http）
- 问题：启用 EMBEDDING_PROVIDER=api 时若在 Node16 运行时直接 fetch is not defined；且 apiEmbed 无超时、无重试。
- 整改动作：apiEmbed 改用 node:https 同款方案并加超时/重试；或文档明确仅支持 Node18+（package.json 已写 engines>=18，但别靠运气）。

【P2-3】向量维度无一致性校验，静默错算
- 位置：backend/src/rag/embedder.js（LOCAL_DIM）、backend/src/rag/retriever.js（cosine 按 a.length 循环）、backend/src/kb/ingest.js
- 问题：local(512) 建好索引后改 api(1536) 未重 ingest，cosine 只遍历前 512 维，维度不匹配不报错只产出错误分数。
- 整改动作：ingest / loadIndex 时校验 config.embedding.dim 与索引 dim 一致，不一致直接抛错而非静默错算。

【P2-4】护栏数字溯源匹配过宽松
- 位置：backend/src/rag/guard.js（context.includes(n)）
- 问题：子串匹配。"5岁"KB 必含"5"、"500ml"撞"500mg"会被误判已溯源放行，放过伪造剂量/百分比。
- 整改动作：数字匹配带单位与词边界（正则边界或结构化比对），不要裸 includes。

【P2-5】内部错误详情泄露给客户端
- 位置：backend/src/routes/chat.js（res.status(500).json({ detail: err.message })）
- 问题：返回 err.message 可能含 LLM 错误原文、文件路径、堆栈片段，便于攻击者探测。
- 整改动作：对外只返回通用错误文案，详细错误记服务端日志。

【P2-6】测试覆盖有大洞
- 位置：backend/test/（仅 rag.test.js）
- 问题：编排层 answer.js、所有 routes/*、ocr/*、kb 门控、ingest 零单测；测试依赖被 .gitignore 的 index.json，未 ingest 就跑挂。
- 整改动作：补编排层与 HTTP 接口的集成测试；测试夹具改为内置样本索引，不依赖 gitignore 的外部 index.json。

【P2-7】代码同步靠手动，易分叉
- 位置：tools/sync-cloudfunction.js（无 CI / 无 git hook / package.json 未引用）
- 问题：backend 改了忘跑 sync → 线上跑旧逻辑；或直接改 cloudfunctions/qa/src → 与 backend 永久不一致。
- 整改动作：加 pre-push 或 CI 校验 diff -rq backend/src/rag cloudfunctions/qa/src/rag 必须为空；或在 package.json 加 predeploy 自动跑 sync。

================================================================
P3 — 清理项（低风险）
================================================================

【P3-1】删除空目录 cloudfunctions/cloudfunction/（历史遗留，0 文件，避免误部署）

【P3-2】清理自托管死代码 utils/request.js 与 utils/auth.js
- 前端主流程已全走 wx.cloud.callFunction，自托管路径不再被调用。README 亦称"不再需要 utils/request"。建议删除或标注"仅自托管备用"。

【P3-3】qa/package.json 补全 dotenv 声明
- config.js 顶部 require('dotenv') 被 try/catch 包住不崩，但 qa/package.json 未声明该依赖。补进 dependencies 或删掉该 require。

【P3-4】OCR 临时文件清理
- 位置：cloudfunctions/qa/index.js downloadFromCloud（写 os.tmpdir() 无 unlink）
- 整改动作：下载落盘后加 try/finally 清理，避免云函数实例复用累积。

【P3-5】文档对齐
- a) README 称知识库"三份"，实际 knowledge_base/ 有 5 个 json（多 web_collected.json、hb_deep_kb.json），更新说明。
- b) cloudfunctions/qa/README 步骤与 sync-cloudfunction.js 对不齐，统一同步说明。

【P3-6】历史页体验（小缺陷）
- 位置：pages/history/history.js
- 只有"清空全部"，无单条删除；长答案用 showModal 展示体验差。可补单条删除 + 独立详情页/滚动视图。

================================================================
盲点提示（本次未深入，后续若维护需关注）
================================================================
- web/ 网页测试版（rag-client.js 是后端防幻觉逻辑的浏览器移植）存在与 backend/src/rag 的二次同步负担，长期维护建议理清关系，避免漂移。

================================================================
建议落地顺序
================================================================
1) P0-1 重建审核索引 + P0-2 HTTP 端点鉴权 + P1-1 补 OCR 依赖（半天可落地，不动架构）
2) 接着 P1-2 / P1-3 / P2 各条按排期推进
3) 最后 P3 清理
