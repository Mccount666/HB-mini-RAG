# 审查报告

**项目**：肝母细胞瘤科普问答小程序（`hepatoblastoma-qa-miniprogram`，appid `wx92ba1d52a0eb2e43`）
**评审日期**：2026-09-11
**报告性质**：架构评审 + 整改验收（本文件为评审方与开发方之间的**单一沟通载体**）
**评审方式**：全量源码逐文件审阅 + diff 比对 + 实跑测试 + 数据面实测 + 限速逻辑行为模拟

---

## 〇、给开发 Agent 的说明

1. 本文件由两份历史文档合并而成（原《架构评审-整改指令书》《整改验收报告》），**以本文件为准**；两份原文件已删除。
2. **第三部分是当前待办清单**，请按优先级处理。第一、二部分是背景与已验收内容，供理解上下文。
3. 评审方在本次合并期间**做了两处改动**（均已标注）：`cloudfunctions/qa/index.js` 环境变量注释补充、`backend/.env.example` 补 `FEEDBACK_RATE_LIMIT`。其余内容均为评审结论，未改动业务代码。
4. 每次修改后请更新第五部分「变更自检清单」，并在提交前确认第六部分的人工检查项。
5. **当前工作区有未提交改动**，见待办 F。

---

## 一、总体评价

**架构定级：上游水平，不需要推倒重来。**

四层架构无循环依赖、反幻觉工程真实落地、数据治理规范、包体控制优秀。以下四点是实测确认的，不是纸面架构：

### 1.1 四层架构

```
① 小程序前端 (pages / components / services)
        ↓ wx.cloud.callFunction({name:'qa'})
② 云函数 qa（唯一后端形态，双入口）
        ↓ sync-cloudfunction.js 单向拷贝
③ 共享 RAG 核心 backend/src/{rag, ocr, config.js}
        ↓ 可插拔
④ 外部依赖（DeepSeek LLM / 本地哈希 embedding / 腾讯云 OCR 或 mock）
```

核心价值：**同一套 RAG 逻辑同时服务小程序端（云函数）与自托管 web 端（`web/rag-client.js`）**，一份逻辑两个出口。

### 1.2 反幻觉六道防线

`backend/src/rag/guard.js` 实测确认以下机制均已落地：

- `UNIT_EQUIV` 单位等价类（mg↔毫克、ml↔mL↔毫升）
- `consumeUnit()` 多字符单位优先 + 斜杠复合单位处理
- `normalizeNumbers()` 全角数字 → 半角、中文数字+单位 → 阿拉伯数字
- `checkGroundedNumbers()` 带词边界环视（避免 `7` 误匹配 `17`）
- `guardAnswer()` 分级处理：`ungrounded_numbers` **不重试直接拒答**，`no_citations` 允许重试一次

分级正确：数字溯源失败属硬错误，重试无意义；引用缺失属可修复，允许一次强化重试。

### 1.3 数据面健康

| 检查项 | 结果 |
|---|---|
| 本地知识库总条数 | 167 条 |
| 已终审条数 | **167 / 167（100%）** |
| `isReviewed()` 双条件 | `reviewed === true && reviewedBy.trim()` |
| 线上索引 `onlyReviewed` | `true` |
| 线上索引维度 | `dim: 512` |
| 线上索引条数 | `items.length: 167` |

`isReviewed()` 要求 `reviewedBy` 非空，意味着**「谁审的」必须留痕**——这是正确的医疗内容治理姿势。

### 1.4 包体控制

| 目录 | 体积 |
|---|---|
| pages | 27 KB |
| components | 11 KB |
| services | 4 KB |
| **前端合计** | **42 KB** |

主包 2MB 限制下连零头都不到，**不需要分包**。`packOptions.ignore` 已排除 backend/web/docs/tools/知识库备份，`lazyCodeLoading: requiredComponents` 已开启。

---

## 二、已验收内容（提交 `e097041` + 后续未提交改动）

**验收结论：9 项整改全部落地；新增发现 3 项，其中 2 项已修、1 项为新增待办（待办 H，优先级最高）。**

测试 **38/38 全绿**（原 29 → +9），`check-sync` 零漂移。

> **重要状态提示**：`cloudfunctions/qa/index.js`、`backend/test/cloudfunction-static.test.js`、`backend/.env.example` 的改动**尚未提交**（见待办 F）。
> **配套阅读**：开发方的回应见 `审查反馈.md`（评审结论与反馈文件配对使用）。

### ✅ P0-1 知识库翻译残留 —— 已闭环

| 检查项 | 结果 |
|---|---|
| 源文件 `lab_reference.json` | `AST/ALT  together 辅助判断肝损伤` → **`AST/ALT 比值可辅助判断肝损伤类型`** ✓ |
| `reviewedBy` 留痕 | `参考标准入库` → **`导师终审（化验参考区间）`** ✓ |
| **线上索引是否重建** | `generatedAt` 从 `2026-09-06T04:21:49Z` → **`2026-09-11T06:49:34Z`** ✓ |
| 线上索引是否含污染词 | `LAB-AST` 文本中 `together` 已消失 ✓ |
| 全库复扫 | 仅剩 `HB-254`/`HB-255` 两处 `Together by St. Jude`（机构专名，**正确保留**）✓ |

**关键验证点是「是否重新 ingest」。** 只改 JSON 不重建索引等于没改——实测 `generatedAt` 已更新，**确认闭环**。

---

### ✅ P0-2 feedback 无鉴权 —— 已闭环

**改动位置**：`cloudfunctions/qa/index.js`

```javascript
// 第 128-132 行
async function handleFeedback(data = {}, identity = '') {
  const feedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT || '5', 10);
  if (rateLimited('fb:' + (identity || 'anonymous'), feedbackLimit)) {
    return { ok: false, error: '提交过于频繁' };
  }
  ...

// 第 206-207 行：rateLimited 加可选 limit 参数（后向兼容）
function rateLimited(ip, limitOverride) {
  const limit = Number.isFinite(limitOverride) ? limitOverride : parseInt(process.env.HTTP_RATE_LIMIT || '20', 10);

// 第 58 行（直调入口）
return handleFeedback(event, callIdentity(event, context));

// 第 280 行（HTTP 入口）
return httpJson(await handleFeedback(data, getSourceIp(event)), 200, cors);
```

**实测行为验证**：

| 验证项 | 结果 |
|---|---|
| 后向兼容（不传 `limitOverride` 走 env） | 通过 ✓ |
| 键隔离（`fb:<id>` 与裸 `<ip>` 独立） | 独立 ✓（已模拟验证） |
| 普通请求桶第 21 次被限 | 通过 ✓ |
| feedback 桶第 6 次被限（限 5） | 通过 ✓ |
| 直调路径也走限速 | 是 ✓ |

**确认无桶冲突**：`handleFeedback` 内部统一加 `'fb:'` 前缀，HTTP 传裸 IP 会变成 `fb:<ip>`，与普通 POST 桶不互相消耗额度。

---

### ✅ P1-2 文档口径漂移 —— 已全部对齐

| 位置 | 整改前 | 整改后 |
|---|---|---|
| `docs/architecture.md:63` | 「默认阈值 **0.35**」 | **「本地向量化默认阈值 0.50」** ✓ |
| `pages/about/about.wxml:19` | 「正式收录 **167 条**」 | **去掉数字** ✓ |
| `README.md:142` | （早期表述） | **「当前已收录 167 条并全部完成导师终审」** ✓ |
| `README.md:154` | 「当前共 **44 条待审**」 | **「当前为 167 条全部已终审，无待审条目」** ✓ |
| `README.md:151` | 「全部条目均为 `reviewed:false` 的演示数据」 | 该表述已移除 ✓ |

实测 `docs/architecture.md:63` 为 `0.50`、`:64` 保持 `0.45`，与 `config.js:46` 的 `embeddingProvider === 'local' ? '0.50' : '0.45'` 完全一致。

---

### ✅ P2-1 依赖与体积 —— 已清理

| 检查项 | 结果 |
|---|---|
| `cloudfunctions/qa/node_modules` | **已整体删除**（原 65 MB，含 32.6 MB 类型定义）✓ |
| `.gitignore` | 新增 **`cloudfunctions/*/node_modules/`** ✓ |
| `dotenv` 声明 | 从云函数 `package.json` 移除 ✓ |

**关于 `dotenv` 的说明**：`cloudfunctions/qa/src/config.js` 仍保留 `try { require('dotenv').config(); } catch (e) {}`。**这是正确的**——`config.js` 是本地与云函数共用文件，`try/catch` 让本地读 `.env`、云函数静默跳过。从云函数依赖移除恰好表达「云函数不需要它」的语义。

---

### ✅ P2-2 死代码 —— 已删除

| 文件 | 状态 |
|---|---|
| `utils/request.js` | **已删除** ✓ |
| `utils/auth.js` | **已删除** ✓（指令书未点名，属主动清理） |
| `utils/` 目录 | **整体移除** ✓ |
| 全项目引用扫描 | 业务代码零引用 ✓ |
| `services/chat.js` 注释 | 同步更新 ✓ |

---

### ✅ P2-3 同步校验 —— 已加钩子

```json
// backend/package.json
"pretest": "node ../tools/check-sync.js",
"test": "node --test",
```

实测 `node ../tools/check-sync.js` → `✓ backend/src 与 cloudfunctions/qa/src 完全一致`，exit 0。

**效果**：跑 `npm test` 前自动校验，静默分叉风险基本消除。

**仍可加强**：若只跑 `node --test` 不走 `npm test`，`pretest` 不触发（见第三部分遗留 D）。

---

### ✅ P2-4 OCR 数字溯源 —— 已补上（超出指令书建议）

指令书建议 A（改提示词）/ B（双向校验）/ C（接受现状）三选一，**整改者做了 A + B**。

**方案 B**（`backend/src/ocr/interpret.js`）：

```javascript
const { validateCitations, checkGroundedNumbers } = require('../rag/guard');
...
const { answer: citedInterpretation } = validateCitations(rawInterpretation, hits.length);
const numberContext = [{ text: rawText }, ...hits.map((h) => ({ text: h.text }))];
const { ungrounded } = checkGroundedNumbers(citedInterpretation, numberContext);
const interpretation = ungrounded.length
  ? '化验单解读结果中出现了无法在报告原文或参考标准中核对的数字。为避免误导，暂不展示自动解读，请带报告咨询主治医生或专科护士。'
  : citedInterpretation;
```

把「家长报告原文」与「命中的参考标准」合并为校验上下文，输出含两者之外数字则整条降级为安全提示。

**方案 A**（`backend/src/rag/prompt.js` OCR_SYSTEM 第 1、2 条）：

```
1. ...不得凭空编造任何指标或数值；不得输出家长报告与参考标准之外的任何数字。
2. 如需说明正常范围或阈值，必须逐字引用 <参考标准> 原文；...
```

**新增测试**（`backend/test/rag.test.js`）正反双向：

```javascript
test('checkGroundedNumbers 支持 OCR 报告原文与参考标准双来源校验', () => {
  const contexts = [
    { text: '化验单原文：AFP 1250.0 ng/mL，ALT 85 U/L。' },
    { text: '成人常见参考区间约 7–40 U/L。' },
  ];
  const ok = checkGroundedNumbers('AFP 1250.0 ng/mL，ALT 85 U/L，高于 7–40 U/L 参考区间 [来源1]。', contexts);
  assert.equal(ok.ungrounded.length, 0);
  const bad = checkGroundedNumbers('AFP 1250.0 ng/mL，ALT 85 U/L，高于 50 U/L 参考区间 [来源1]。', contexts);
  assert.ok(bad.ungrounded.includes('50'));
});
```

---

### ✅ P1-1 限速机制 —— 部分改善，根本限制仍在

**已改善**：主键不再是纯 IP。直调路径用 OPENID（`call:<openid>`），feedback 用 `fb:<identity>`。

**根本限制未变**（本轮未处理，属预期）：
- `rateBuckets` 仍是**实例内存 Map**，多实例下实际限速 ≈ `N × 20/分钟`
- 冷启动后计数清零
- **云函数并发上限仍未配置**

---

### ✅ 2.10 匿名身份共桶问题 —— 已修复，但引入新的绕过风险（未提交）

**评审期间发现的问题**：`getSourceIp()` 兜底值是 `'unknown'`（真值），导致 `identity || 'anonymous'` 实际走 `'unknown'`，HTTP 入口所有取不到 IP 的访客会共享 `fb:unknown` 一个桶，第 6 个提交者即被限。

**修复实现**（`cloudfunctions/qa/index.js`，当前为未提交状态）：

```javascript
function feedbackRateIdentity(identity = '', data = {}) {
  const id = String(identity || '').trim();
  if (id && id !== 'anonymous' && id !== 'unknown') return id.slice(0, 120);
  const seed = [data.openid, data.userId, data.q, data.comment, data.topicKey, data.topicLabel, data.mode]
    .map((v) => String(v || '').trim())
    .filter(Boolean)
    .join('|');
  const fallback = seed || `${Date.now()}:${Math.random()}`;
  const hash = crypto.createHash('sha256').update(fallback).digest('hex').slice(0, 16);
  return `anon:${hash}`;
}

async function handleFeedback(data = {}, identity = '') {
  const feedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT || '5', 10);
  if (rateLimited('fb:' + feedbackRateIdentity(identity, data), feedbackLimit)) {
    return { ok: false, error: '提交过于频繁' };
  }
  ...
}
```

#### 修复的正向效果（实测确认）

| 场景 | 实测结果 |
|---|---|
| 不同访客（内容不同） | **分入不同桶** ✓ 不再互相误限 |
| 同一访客重复提交**相同内容** | **落同一桶** ✓ 防刷能力保留 |
| 完全空数据 | 时间+随机兜底，**分桶** ✓ |
| 有真实 OPENID / IP | 直接用作身份，**截断 120 字符** ✓ |

#### ⚠️ 但引入了新的绕过路径（实测：限速可被完全绕开）

**核心问题**：`feedbackRateIdentity` 把**反馈内容**当成了限速身份。而内容是**攻击者完全可控**的。

**攻击模拟实测**（固定 `unknown` 身份，每次请求内容尾部加随机字符）：

| 方案 | 1000 次请求 | 放行 | 桶数 |
|---|---|---|---|
| **修复后**（内容哈希分桶） | 1000 | **1000（全部放行）** | **1000** |
| 修复前（`fb:unknown` 单桶） | 1000 | 5 | 1 |

**修复后限速完全失效**——桶数随请求数线性增长，攻击者每次改一个字就能拿到全新额度。

**这直接影响 P0-2 的防护目标**：`feedback` 是**计费写入**，P0-2 当初就是为防灌水计费而加的限速。修复后这个防护形同虚设。**属于「用一个可用性隐患，换来了一个安全回退」。**

#### 建议改法：双层限速（内容桶 + 全局兜底桶）

保留内容哈希（解决共桶误限），**另加一层全局兜底桶**兜住内容变体的洪峰：

```javascript
async function handleFeedback(data = {}, identity = '') {
  const feedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT || '5', 10);
  const globalFeedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT_GLOBAL || '200', 10);

  // 层 1：按身份/内容分桶（防止同一内容刷屏、避免匿名共桶误限）
  if (rateLimited('fb:' + feedbackRateIdentity(identity, data), feedbackLimit)) {
    return { ok: false, error: '提交过于频繁' };
  }
  // 层 2：全局兜底（无论身份如何，实例级总闸；兜住内容变体攻击）
  if (rateLimited('fb:__global__', globalFeedbackLimit)) {
    return { ok: false, error: '服务繁忙，请稍后再试' };
  }
  ...
}
```

**实测效果**：1000 次内容变体攻击 → 放行 **200** 次、拦截 **800** 次 ✓

**注意**：全局桶仍然是**实例级**的（同 `rateBuckets` 的限制），多实例下总闸 ≈ `N × 200/分钟`。但配合**待办 B 的云函数并发上限**，两道一起才完整。

**补充说明**：`getSourceIp()` 实测**从不返回空值**（永远返回 IP 或字符串 `'unknown'`），因此 `feedbackRateIdentity` 中 `!id` 的判空分支实际走不到，但保留无害。

**回归测试**（`backend/test/cloudfunction-static.test.js`，新增）：采用静态源码断言防止回退旧写法，思路正确，但**当前断言只验证了「不共桶」，未覆盖「限速不得被内容变体绕过」**——建议补一条。

**唯一提醒**：该文件与 `index.js` **尚未提交**，请在下一轮提交时一并纳入。

---

### ✅ 2.11 `FEEDBACK_RATE_LIMIT` 文档遗漏 —— 已由评审方修复

**问题**：`handleFeedback` 读取 `process.env.FEEDBACK_RATE_LIMIT`，但该变量**未记录在任何文档中**，运维在控制台配置时不知道其存在。

| 位置 | 修复前 | 修复后 |
|---|---|---|
| `cloudfunctions/qa/index.js` 顶部注释块（14-17 行） | 未记录 | **已补第 18 行** ✓ |
| `backend/.env.example`（HTTP 防护段） | 未记录 | **已补 `FEEDBACK_RATE_LIMIT=5`** ✓ |
| `README.md` | 不需要（仅简略提及，详细清单在 .env.example） | 保持不动 ✓ |

**修复内容**：

```javascript
// cloudfunctions/qa/index.js 第 18 行（新增）
//   FEEDBACK_RATE_LIMIT: 反馈提交限速，每身份每分钟上限（默认 5），超出返回 { ok:false, error:'提交过于频繁' }
```

```bash
# backend/.env.example 第 63-65 行（新增）
# 反馈提交限速：每身份每分钟上限（默认 5，超出返回 { ok:false, error:'提交过于频繁' }）。
# 云函数按 OPENID / IP 归因；拿不到身份时按反馈内容哈希分桶，避免所有匿名访客共用一个桶被误限。
FEEDBACK_RATE_LIMIT=5
```

> **注意**：若采纳 2.10 的双层限速建议，此处需再补 `FEEDBACK_RATE_LIMIT_GLOBAL=200`。

**验证**：改动后跑测试 **38/38 全绿**，无回归。

---

## 三、当前待办清单（按优先级）

> 原「遗留 A」（`fb:unknown` 共桶）已修复，**但修复引入了限速绕过风险**，见 2.10 与待办 H。
> 原「遗留 D」（`FEEDBACK_RATE_LIMIT` 文档）已闭环，见 2.11。
> 以下为**当前仍需处理**的条目。

### 待办 H｜反馈限速可被「内容变体」完全绕过 —— 安全回退

**级别**：**P0（建议优先于其他项，因为它回退了一个已修复的 P0 防护）**
**位置**：`cloudfunctions/qa/index.js`，`feedbackRateIdentity()` 与 `handleFeedback()`

**问题**：修复把「反馈内容」当成了限速身份，而内容是攻击者完全可控的。

**实测证据**（固定 `unknown` 身份，每次请求内容尾部加随机字符）：

| 方案 | 1000 次请求 | 放行 | 桶数 |
|---|---|---|---|
| 修复后 | 1000 | **1000（全部放行）** | 1000 |
| 修复前 | 1000 | 5 | 1 |

**性质判定**：这是**安全回退**。`feedback` 集合按量计费，P0-2 当初正是为防灌水计费而加限速；修复后该防护失效。

**建议改法**：双层限速（保留内容桶解决共桶误限，另加全局兜底桶压制变体攻击）。

```javascript
async function handleFeedback(data = {}, identity = '') {
  const feedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT || '5', 10);
  const globalFeedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT_GLOBAL || '200', 10);

  // 层 1：按身份/内容分桶（防同一内容刷屏 + 避免匿名共桶误限）
  if (rateLimited('fb:' + feedbackRateIdentity(identity, data), feedbackLimit)) {
    return { ok: false, error: '提交过于频繁' };
  }
  // 层 2：全局兜底（实例级总闸，兜住内容变体攻击）
  if (rateLimited('fb:__global__', globalFeedbackLimit)) {
    return { ok: false, error: '服务繁忙，请稍后再试' };
  }
  ...
}
```

**实测效果**：1000 次内容变体攻击 → 放行 **200**、拦截 **800** ✓

**配套动作**：
- `.env.example` 补 `FEEDBACK_RATE_LIMIT_GLOBAL=200`
- `index.js` 顶部注释块补该变量说明
- 为 `cloudfunction-static.test.js` 补一条断言：**限速不得仅依赖内容哈希**（防止回退）

> 全局桶仍是实例级（同 `rateBuckets` 限制），多实例下总闸 ≈ `N × 200/分钟`。**需与待办 B 的并发上限配合才完整。**

---

### 待办 B｜云函数并发上限未配置 —— 成本兜底缺失

**级别**：P1
**位置**：云开发控制台配置项（非代码）

**问题**：`rateBuckets` 是实例内存 Map，云函数水平扩容下：
- 请求分散到 N 个实例，每个独立计数
- **实际限速 ≈ `N × 20/分钟`**，N 由攻击者并发量决定
- 冷启动后计数清零

**这条是当前最值得优先处理的成本风险**——`feedback` 与 `qa` 都按量计费。

**关键判断**：**限速可以绕，并发上限绕不掉。** 配置云函数最大并发是把最坏情况成本封顶的**唯一手段**。

**建议**：
1. **立即**：在云开发控制台为 `qa` 云函数配置**最大并发实例数**（按预算测算）
2. **短期**：限速计数挪到云数据库（集合 + 原子自增 + TTL），实现跨实例共享
3. **中期**：按 OPENID 为主键，避免 IP 在移动网络下的 NAT 共享误伤

---

### 待办 C｜`minCoverage=0`，近邻问题最后防线是模型自觉

**级别**：P1（需数据定标，非直接改代码）
**位置**：`backend/src/config.js` 第 52 行

```javascript
minCoverage: parseFloat(process.env.RETRIEVAL_MIN_COVERAGE || '0'),
```

**代码注释已解释关闭原因**（第 48-51 行）：口语化提问覆盖率天然偏低，无法与近邻问题统计分隔，故只作 eval 观测指标；近邻问题由「严格提示词拒答 + 生成后护栏」兜底。

**这个判断合理，不建议无脑打开。** 但需明确风险承担：

- 提示词防线的强度取决于模型是否听话（`temperature: 0` 下相对稳定，非绝对）
- 护栏只拦「数字无出处」和「无引用」，**拦不住「有引用但答非所问」**

即：**近邻问题的最后一道防线是模型的自觉性**。对医疗科普偏薄。

**建议改法（不要直接改成非零）**：

1. 跑 `npm run eval`（`tools/eval.js`），对同一批测试问题分别取 `minCoverage = 0` 和 `0.3`
2. 比较**误拒率**与**近邻误答率**两条曲线
3. 仅在数据支持时启用，且建议保留 `0` 为默认、通过 `.env` 按场景开启

---

### 待办 F｜未提交改动需纳入版本控制

**级别**：P2（流程性，但易漏）
**位置**：工作区

当前 `git status` 显示以下**改动尚未提交**：

| 文件 | 状态 | 内容 |
|---|---|---|
| `cloudfunctions/qa/index.js` | `M` | `feedbackRateIdentity()` 匿名分桶 + `FEEDBACK_RATE_LIMIT` 注释补充 |
| `backend/test/cloudfunction-static.test.js` | `??` | 匿名共桶的静态回归测试 |
| `backend/.env.example` | `M` | `FEEDBACK_RATE_LIMIT=5` 补充 |
| `审查报告.md` | `??` | 本文件 |
| `审查反馈.md` | `??` | 开发方反馈文件 |
| `化验单-测试样张.png` | `??` | 测试素材，**建议加入 `.gitignore`** |
| `知识库备份-2026-09-02/` | `??` | 备份目录，**建议加入 `.gitignore`** |

**特别注意**：
- `index.js` 与 `cloudfunction-static.test.js` 是**功能性修复 + 回归测试**，必须提交
- 提交后需重新**上传部署云函数**（改的是云函数入口本身，不在 `backend/src`，`check-sync` 不覆盖此文件）

---

### 待办 G｜`utils/` 目录删除后无引用残留 —— 已复核，无需处理

**级别**：P3（留痕）

实测确认：全项目业务代码对 `utils/request` 与 `utils/auth` 的引用数为 **0**。`.mimosa/` 目录下有 4 处历史审查报告残留字符串，不影响运行，且已被 `.gitignore` 忽略。

---

## 四、测试与校验实跑结果

**最后一次实跑（含评审方对 `.env.example` / `index.js` 的文档补充后）**：

| 项目 | 结果 |
|---|---|
| `node --test`（backend） | **38/38 pass**，0 fail ✓ |
| 新增测试数 | 29 → **38**（+9）✓ |
| `node tools/check-sync.js` | **完全一致**，exit 0 ✓ |
| 线上索引 `onlyReviewed` | `true` ✓ |
| 线上索引维度 | `dim: 512` ✓ |
| 线上索引条数 | `items.length: 167` ✓ |
| 线上索引 `generatedAt` | `2026-09-11T06:49:34Z`（已重建）✓ |
| 前端包体 | **42 KB**，无变化 ✓ |

**新增的 9 条测试构成**（原 29 → 38）：

- 8 条：OCR 双来源校验 + 各类数字溯源边界（小数点不作战通配符、相邻数字各自溯源、单位大小写、中文单位等价类、全角数字、mmol/L 不撞毫米、斜杠复合单位）
- 1 条：`cloudfunction-static.test.js` —— 云函数匿名共桶的静态回归

**环境说明**：`npm test` 因当前 shell PATH 问题无法直接调用（`npm` 不在可执行路径），改用等价的 `node --test` 执行，结果相同。不影响验收结论。

---

## 五、变更自检清单

每次改动后请对照本表自查。

### 改动类型 → 必做动作

| 改动内容 | 必做动作 | 漏做的后果 |
|---|---|---|
| 改 `backend/data/knowledge_base/*.json` | **必须重新 `npm run ingest`**，再 `npm run sync` + 上传云函数 | 线上检索到的还是旧文本，**静默** |
| 改 `backend/src/**`（rag / ocr / config） | **必须 `npm run sync`** | 云函数跑旧代码，**无任何报错** |
| 改 `cloudfunctions/qa/index.js`（云函数入口本身） | **必须重新上传部署云函数**（不在 sync 范围内） | 线上跑旧入口，**静默** |
| 改知识库条数 | 核对 `about.wxml`、`README.md`、`OVERVIEW.md` 口径 | 对用户的不实陈述 |
| 改配置默认值 | 同步 `docs/architecture.md`、`.env.example` | 后续维护者按错误基线调参 |
| 新增/修改环境变量 | **同步 `index.js` 顶部注释块 + `.env.example`** | 运维盲区（曾发生在 `FEEDBACK_RATE_LIMIT`） |
| 改限速/防刷逻辑 | **必须做「绕过测试」**——站在攻击者角度试能否规避 | 防护形同虚设（曾发生在内容哈希分桶） |

> **本项目历史上已出现三次「改了代码忘改配套」**：
> 1. `docs/architecture.md` 阈值 0.35 vs 代码 0.50
> 2. `about.wxml` 167 条 vs `README.md` 44 条待审
> 3. `FEEDBACK_RATE_LIMIT` 新增但未记录文档
>
> **以及一次「修复引入新问题」**：匿名共桶修复 → 限速可被内容变体绕过（待办 H）。
>
> **这三处漂移已全部修复，但纪律比一次修 bug 更重要。** 建议把本表固化为提交前习惯，尤其是新增的「绕过测试」一条——**防刷逻辑写完，一定要站在攻击者角度试一遍**。

### 提交前自检命令

```bash
cd backend && npm test          # 会自动跑 pretest 校验同步
node ../tools/check-sync.js     # 单独校验漂移
node ../tools/eval.js           # 检索评测（改检索/阈值时必跑）
```

### 已确认的三道防线

1. `pretest` 钩子 → 跑 `npm test` 时自动校验同步状态
2. `predeploy` 脚本 → `sync && check:sync`
3. `.gitignore` → `cloudfunctions/*/node_modules/` 已显式排除

---

## 六、提审前人工检查项（代码层面无法验证）

以下事项**必须人工走一遍**，是提审被拒的高发区：

- [ ] 医疗健康类目资质是否已提交并通过
- [ ] 隐私保护指引是否已在微信后台填写与实际收集信息一致
- [ ] `__usePrivacyCheck__: true` 与后台隐私协议配置是否匹配
- [ ] `app.js` 云开发 `env` 是否为生产环境 ID
- [ ] `project.config.json` 的 `appid` 是否正确
- [ ] 云函数 `qa` 的环境变量（`LLM_API_KEY`、`HTTP_SHARED_SECRET` 等）是否已在生产环境配置
- [ ] **云函数最大并发实例数是否已配置**（见遗留 B）
- [ ] 免责声明在各页面是否可见且表述合规

---

## 七、评审结论

**架构本身可以上线，代码质量在及格线以上，反幻觉工程做得比多数商用 RAG 扎实。**

**本轮状态：原 9 项整改全部闭环；新增发现 3 项（2.10 匿名共桶、2.11 文档遗漏、待办 H 限速绕过），其中 2.11 已修、2.10 的修复引入了待办 H。**

### 关于 2.10 → 待办 H 的判断

修复方向正确（原共桶确实是问题），但**换错了维度**：把攻击者可控的「内容」当成了身份，导致限速可被内容变体完全绕过。

- **修复前**：可用性隐患（匿名访客共桶误限）
- **修复后**：安全回退（限速失效，计费写入重新暴露）

**建议按 2.10 的双层限速方案补上全局兜底桶**，两边都能兼顾。实测该方案可将 1000 次变体攻击压到 200 次放行。

### 当前待办优先级

| 顺序 | 项目 | 成本 | 理由 |
|---|---|---|---|
| 1 | **待办 H**（反馈限速双层化） | 低（约 6 行） | **回退了一个 P0 防护**，`feedback` 是计费写入 |
| 2 | **待办 B**（配置云函数并发上限） | 极低（控制台操作） | 唯一能把成本封顶的手段；**限速终究可绕，并发上限绕不掉** |
| 3 | 待办 F（提交未提交改动） | 极低 | 修复与回归测试尚未入库 |
| 4 | 待办 C（`minCoverage` 定标） | 中（需跑 eval） | 策略决策，需数据支撑 |

**提审建议**：待办 H、B、F 完成后即可提交审核。第六部分的人工检查项请务必逐条走一遍。

### 一句提醒

本项目历史上出现过**三次**「文档/代码漂移」（阈值、条数、环境变量），其中第三次就发生在本轮整改中。**变更纪律比某一次修 bug 更重要**——建议把第五部分的自检清单固化为提交前习惯。

---

*本文件为评审方与开发方之间的单一沟通载体，与 `审查反馈.md` 配对使用。*
*文档版本：2026-09-11，合并自《架构评审-整改指令书》与《整改验收报告》。*
