# 审查反馈

**反馈对象**：`审查报告.md`（原《整改验收报告-2026-09-11.md》已合并入该文件）
**被验收提交**：`e097041`（架构评审整改：清空上线前 P0 风险）
**本反馈用途**：说明我（开发方）在阅读评审意见后所做的调整，供后续 agent 或人工审查过目。
**反馈时间**：2026-09-11
**配套文件**：`审查报告.md`（评审方，与本文件配对使用）

---

## 〇、文件名与版本说明

本文件原名计划为 `审查反馈-2026-09-11.md`，**实际落盘为 `审查反馈.md`**（无日期后缀），以与实际文件名为准。

评审方原《整改验收报告-2026-09-11.md》与《架构评审-整改指令书-2026-09-11.md》**已合并为 `审查报告.md` 并删除原文件**，因此本反馈中引用的编号体系（遗留 A/B/C/D）对应到 `审查报告.md` 为：

| 本反馈旧编号 | `审查报告.md` 当前位置 |
|---|---|
| 遗留 A（匿名共桶） | 2.10（已修复）→ 其衍生问题见**待办 H** |
| 遗留 B（并发上限） | **待办 B**（未处理） |
| 遗留 C（minCoverage） | **待办 C**（未处理） |
| 遗留 D（文档遗漏） | 2.11（**评审方已代为修复**） |

---

## 一、结论摘要

我已阅读评审方当时的验收报告（现为 `审查报告.md` 第二、三部分）。报告确认上一轮主体整改质量合格、P0 风险已清空，同时指出唯一建议顺手修复的遗留项：

- **遗留 A**：`cloudfunctions/qa/index.js` 中 feedback 限速在 `identity` 为空时会退化为统一的 `fb:anonymous` 桶，可能导致 HTTP 入口取不到 IP 时，多个匿名用户共享同一个限速桶，从而造成误限流。

针对该项，我已完成补充整改：

1. 新增 `feedbackRateIdentity(identity, data)`，避免匿名兜底共桶。
2. 将 feedback 限速键从旧写法 `fb:${identity || 'anonymous'}` 改为 `fb:${feedbackRateIdentity(identity, data)}`。
3. 新增静态回归测试 `backend/test/cloudfunction-static.test.js`，防止后续退回旧写法。
4. 已运行语法检查、同步校验、后端测试和空白检查，均通过。

> **评审方回应**：修复方向正确，但**引入了新的绕过路径**——详见 `审查报告.md` 待办 H。简言之：把攻击者可控的「反馈内容」当作限速身份后，每次改内容即可获得全新额度，实测 1000 次变体请求全部放行。建议补一层全局兜底桶（双层限速）。**本反馈结论中的「已完成补充整改」应修正为「已完成，但需追加一层全局兜底」。**

---

## 二、时间轴

### 2026-09-11 15:27 左右：发现并读取验收报告

通过文件扫描定位到最新验收报告：

- `审查报告.md`（当时为 `整改验收报告-2026-09-11.md`，现已合并）

报告结论显示：

- 上一轮 9 项整改中，8 项验收通过，1 项需补充说明。
- P0 风险已清空，可以进入提审流程。
- 唯一建议下一轮顺手修复的是 **遗留 A：anonymous 共桶问题**。

### 2026-09-11 15:30 左右：确认遗留 A 的具体位置

读取 `cloudfunctions/qa/index.js` 后确认旧逻辑位于 feedback 入库入口：

```js
async function handleFeedback(data = {}, identity = '') {
  const feedbackLimit = parseInt(process.env.FEEDBACK_RATE_LIMIT || '5', 10);
  if (rateLimited('fb:' + (identity || 'anonymous'), feedbackLimit)) {
    return { ok: false, error: '提交过于频繁' };
  }
  ...
}
```

问题点：

- 当 `identity` 为空时，所有匿名请求都会使用 `fb:anonymous`。
- 小程序直调通常有 `OPENID`，风险较低。
- HTTP 入口若因为云接入事件结构变化取不到 IP，则 `getSourceIp(event)` 会退化为 `unknown` 或空值，可能导致多个访客共用同一个 feedback 限速桶。
- 这是可用性隐患，不是数据泄露或鉴权绕过问题。

### 2026-09-11 15:35 左右：完成代码补充整改

在 `cloudfunctions/qa/index.js` 中新增了匿名限速身份归一函数：

```js
function feedbackRateIdentity(identity = '', data = {}) {
  const id = String(identity || '').trim();
  if (id && id !== 'anonymous' && id !== 'unknown') return id.slice(0, 120);
  const seed = [data.openid, data.userId, data.q, data.comment, data.topicKey, data.topicLabel, data.mode]
    .map((v) => String(v || '').trim())
    .filter(Boolean)
    .join('|');
  const fallback = seed || `${Date.now()}:${Math.random()}`;
  const hash = crypto.createHash('sha256').update(fallback).digest('hex').slice(0, 16);
  return `anon:${hash}`;
}
```

并将 feedback 限速调用改为：

```js
if (rateLimited('fb:' + feedbackRateIdentity(identity, data), feedbackLimit)) {
  return { ok: false, error: '提交过于频繁' };
}
```

调整后的行为：

- 有效身份（如 `OPENID`、真实 IP）继续直接作为限速维度。
- `anonymous`、`unknown`、空字符串不再进入统一桶。
- 匿名兜底会基于反馈内容生成短哈希键。
- 如果反馈内容也为空，则用当前时间和随机数生成兜底，避免多个匿名请求被统一挤入一个桶。

### 2026-09-11 15:40 左右：新增防回归测试

新增测试文件：

- `backend/test/cloudfunction-static.test.js`

测试目的：

- 确认云函数入口存在 `feedbackRateIdentity(identity = '', data = {})`。
- 确认代码显式排除了 `anonymous` 和 `unknown` 作为稳定身份。
- 确认匿名兜底通过 `crypto.createHash('sha256')` 生成区分度。
- 确认旧写法 `rateLimited('fb:' + (identity || 'anonymous'))` 不再出现。

测试代码核心断言：

```js
assert.match(src, /function feedbackRateIdentity\(identity = '', data = \{\}\)/);
assert.match(src, /id !== 'anonymous' && id !== 'unknown'/);
assert.match(src, /crypto\.createHash\('sha256'\)\.update\(fallback\)/);
assert.doesNotMatch(src, /rateLimited\('fb:' \+ \(identity \|\| 'anonymous'\)/);
```

### 2026-09-11 15:45 左右：完成验证

已执行以下验证：

```bash
node --check cloudfunctions/qa/index.js
cd backend && npm test
node tools/check-sync.js
git diff --check
```

验证结果：

- `node --check cloudfunctions/qa/index.js`：通过。
- `cd backend && npm test`：通过，**38/38 pass**。
- 新增测试 `cloudfunctions/qa feedback 匿名兜底不再共用 fb:anonymous 单桶`：通过。
- `node tools/check-sync.js`：通过，`backend/src` 与 `cloudfunctions/qa/src` 完全一致。
- `git diff --check`：通过，无空白错误。
- 旧写法扫描：业务代码未发现 `rateLimited('fb:' + (identity || 'anonymous'))` 残留；`fb:anonymous` 仅出现在新增测试名称/断言中，用于防回归。

---

## 三、变更文件清单

本次针对验收报告新增要求实际修改/新增的文件：

| 文件 | 类型 | 说明 |
|---|---|---|
| `cloudfunctions/qa/index.js` | 修改 | 新增 `feedbackRateIdentity`，修复匿名 feedback 共桶隐患 |
| `backend/test/cloudfunction-static.test.js` | 新增 | 增加静态回归测试，防止退回旧匿名桶写法 |
| `审查反馈.md` | 新增 | 本反馈文件，记录针对验收报告的补充整改说明 |

> **评审方补充**：本轮改动**尚未提交**（见 `审查报告.md` 待办 F）。另评审方同日修改了 `cloudfunctions/qa/index.js`（补 `FEEDBACK_RATE_LIMIT` 注释）与 `backend/.env.example`（补该变量），详见 `审查报告.md` 2.11。

---

## 四、当前仍未处理的报告遗留项

验收报告还列了 B/C/D，但这些不是本轮必须马上动代码的项：

| 编号 | 报告级别 | 状态 | 原因 |
|---|---|---|---|
| B | P1 | 未在代码层继续处理 | 云函数并发上限需要在云开发控制台配置，属于部署/运维决策，不是仓库内代码能完全验证的改动 |
| C | P1 | 未修改默认值 | `minCoverage=0` 需要跑 eval 对比 0 与 0.3 后再决策，贸然改动可能误杀真实家长口语问题 |
| D | P3 | **评审方已代为修复** | 原「D」指 `FEEDBACK_RATE_LIMIT` 未记录文档。评审方已补 `index.js` 注释块与 `.env.example`（见 `审查报告.md` 2.11）。本反馈原写的「D 未继续加 CI/pre-commit」为另一独立问题，对应 `审查报告.md` 中的工程增强项，非阻塞 |

> **评审方补充**：B、C 的处理判断正确，无需现在动手。但**新增待办 H** 需优先处理——限速可被内容变体绕过（见 `审查报告.md`）。

---

## 五、提交状态说明

截至写入本反馈文件时，本轮针对验收报告的补充整改尚未提交到 GitHub。

预计应提交的文件：

- `cloudfunctions/qa/index.js`
- `backend/test/cloudfunction-static.test.js`
- `backend/.env.example`（评审方补充的 `FEEDBACK_RATE_LIMIT`）
- `审查报告.md`（评审方文档）
- `审查反馈.md`（本文件）

仍建议继续排除的本地未跟踪项：

- `化验单-测试样张.png`
- `知识库备份-2026-09-02/`

---

## 六、给后续审查 agent 的重点检查建议

请重点核对以下几点：

1. `cloudfunctions/qa/index.js` 中是否已不存在旧写法：

   ```js
   rateLimited('fb:' + (identity || 'anonymous'), feedbackLimit)
   ```

2. 匿名身份是否不会稳定落到同一个 `fb:anonymous` 或 `fb:unknown` 桶。
3. 有效身份路径是否仍然保留：小程序直调用 `OPENID`，HTTP 入口用来源 IP。
4. 新增静态测试是否能防止后续回退。
5. 本次修复不改变主问答限速逻辑，也不改变 feedback 入库字段结构，因此对线上数据结构应为低风险改动。
6. **【评审方追加·重要】限速是否可被「内容变体」绕过**——用一个固定匿名身份、每次请求内容加随机后缀，验证是否会被限流。当前实现**可通过**（即会被绕过），需补全局兜底桶。详见 `审查报告.md` 待办 H。

---

## 七、2026-09-19 待办 H 整改记录（双层限速已落地）

**结论：待办 H 已按评审方建议方案修复并通过绕过测试，测试 40/40 全绿。**

### 改动内容

1. `cloudfunctions/qa/index.js` `handleFeedback` 增加层 2 全局兜底桶 `fb:__global__`，读 `FEEDBACK_RATE_LIMIT_GLOBAL`（默认 200），超出返回 `{ ok:false, error:'服务繁忙，请稍后再试' }`；层 1 内容桶保留在前。
2. `index.js` 顶部注释块、`backend/.env.example` 均已补 `FEEDBACK_RATE_LIMIT_GLOBAL` 说明（按自检清单「新增环境变量同步文档」执行）。
3. `backend/test/cloudfunction-static.test.js`：
   - 静态断言：全局桶必须存在、须读 `FEEDBACK_RATE_LIMIT_GLOBAL`、层 1 必须先于层 2 判断；
   - **新增行为级绕过测试**（按自检清单「改限速必须做绕过测试」）：固定匿名身份，同内容连发验证层 1（第 4 次「提交过于频繁」）；50 次内容变体攻击验证层 2（恰好只放行全局剩余额度 7 次，其余 43 次「服务繁忙」）。

### 绕过测试实测（攻击者视角）

| 攻击手法 | 本轮修复前（2.10 版） | 本轮修复后 |
|---|---|---|
| 固定匿名身份 + 50 次内容变体 | 50/50 全放行（桶数线性增长） | **放行 7（=全局剩余额度），拦 43** |
| 同一内容连发 | 第 4 次被拦 | 第 4 次仍被拦（层 1 行为不变） |

### 验证

- `node --check cloudfunctions/qa/index.js` 通过
- `node tools/check-sync.js` 通过（backend/src 与 cloudfunctions/qa/src 一致）
- `cd backend && npm test`：**40/40 pass**（38 → 40）
- 全局桶仍为实例级（同 `rateBuckets` 限制），与待办 B 并发上限配合才完整——该项待用户在云开发控制台配置

### 部署提醒

`index.js` 是云函数入口本身（不在 sync 范围），**提交后需重新上传部署 qa 云函数**方可在线上生效。
